# Converter Module

A Python module for common unit conversions.

## Functions

- `celsius_to_fahrenheit(c)` -- converts Celsius to Fahrenheit
- `fahrenheit_to_celsius(f)` -- converts Fahrenheit to Celsius
- `kilometers_to_miles(km)` -- converts kilometers to miles
- `miles_to_kilometers(mi)` -- converts miles to kilometers

## Running Tests

```bash
pytest tests/
```

## CI/CD

This repository uses GitHub Actions to run the test suite automatically.
